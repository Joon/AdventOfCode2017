import IPuzzleCalculator from "./IPuzzleCalculator";

export default class PuzzleCalculator6 implements IPuzzleCalculator
{
    CalcPart1(input: string): string {
        if (input.length == 0)
            return "";
    }
    CalcPart2(input: string): string {
        if (input.length == 0)
        return "";
}

}